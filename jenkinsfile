pipeline {
    agent any
    stages {

        stage('Verify tooling') {
            env.PATH = "${env.PATH}:/opt/bin/docker"
            steps {
                sh 'docker info'
                  sh ' docker version'
                    sh 'docker compose version'
                
            }
        }
    }
}
