pipeline {
    agent any
    stages {

        stage('Verify tooling') {
            steps {
                sh 'docker info'
                  sh ' docker version'
                    sh 'docker compose version'
                      sh 'curl --version'
            }
        }
    }
}
