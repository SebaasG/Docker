pipeline {
    agent any
    environment {
        PATH = "$PATH:/opt/bin/docker"
    }
    stages {
        stage('Verify tooling') {
            steps {
                script {
                    sh 'docker info'
                    sh 'docker version'
                    sh 'docker-compose version'
                }
            }
        }
    }
}
